name: test

on: push

jobs:
  testTag:
    runs-on: ubuntu-latest

    steps:
    - name: echo git tag
      run: echo ${{ github.event.release.tag_name }}
      
    - name: echo git tag
      run: echo ${{ github.ref_name }} + " " + ${{ github.ref }}